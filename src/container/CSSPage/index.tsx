import * as React from "react";
import { Background, Layout, Head, Body, Border, Cursor } from "./style";

/**
 * 整理css读书笔记用
 */
function CSSPage() {
  return (
    <Background>
      <Layout>
        ok
        <Head></Head>
        <Body>
          <Border>border</Border>
          <Cursor>cursor</Cursor>
        </Body>
      </Layout>
    </Background>
  );
}

export default CSSPage;
